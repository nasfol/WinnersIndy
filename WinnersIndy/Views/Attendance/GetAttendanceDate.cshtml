@model IEnumerable<WinnersIndy.Data.Attendance>

@{
    ViewBag.Title = "GetAttendanceDate";
}

<h2>GetAttendanceDate</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        @*<th>
                @Html.DisplayNameFor(model => model.ChildrenClassId)
            </th>*@
        <th>
            @Html.DisplayNameFor(model => model.AttendanceDate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            @*<td>
                    @Html.DisplayFor(modelItem => item.ChildrenClassId)
                </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.AttendanceDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.AttendanceId }) |
                @Html.ActionLink("Details", "GetClassAttendance", new { id = item.AttendanceId })
                @* @Html.ActionLink("Delete", "Delete", new { id=item.AttendanceId })*@
            </td>
        </tr>
    }

</table>
